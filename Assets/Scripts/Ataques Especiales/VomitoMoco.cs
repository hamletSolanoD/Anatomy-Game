using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VomitoMoco : AtaqueEspecial
{
    public override void Ataque(Character Enfocado)
    {
        Debug.Log("Usando Vomito Moco");

    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
